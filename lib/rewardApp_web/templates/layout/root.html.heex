<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <meta name="csrf-token" content={csrf_token_value()}>
    <%= live_title_tag assigns[:page_title] || "RewardApp", suffix: " · Phoenix Framework" %>
    <link phx-track-static rel="stylesheet" href={Routes.static_path(@conn, "/assets/app.css")}/>
    <script defer phx-track-static type="text/javascript" src={Routes.static_path(@conn, "/assets/app.js")}></script>
  </head>
  <body>
    <header class="bg-primary">
      <nav class="navbar navbar-expand-lg container">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon text-white"></span>
          </button>
          <div class="collapse navbar-collapse flex-row-reverse" id="navbarNav">
            <ul class="navbar-nav">
            <%= if @current_user do %>
              <li class="nav-item">
                <%= link "Log out", to: Routes.session_path(@conn, :delete, @current_user), method: "delete", class: "nav-link text-white"  %>
              </li>
              <li class="nav-item">
                <%= link "List of users", to: Routes.user_path(@conn, :index), class: "nav-link text-white" %>
              </li>
              <li class="nav-item">
                <%= link "Add reward", to: Routes.reward_path(@conn, :new), class: "nav-link text-white" %>
              </li>
              <li class="nav-item">
                <%= link "Rewards", to: Routes.reward_path(@conn, :index), class: "nav-link text-white" %>
              </li>
              <li class="nav-item text-white p-2">
                <%= @current_user.total_points %>
              </li>
              <li class="nav-item text-white p-2">
                <%= @current_user.name %>
              </li>
              <% else %>
              <li class="nav-item">
                <%= link "Register", to: Routes.user_path(@conn, :new), class: "nav-link text-white" %>
              </li>
              <li class="nav-item">
                <%= link "Log in", to: Routes.session_path(@conn, :new), class: "nav-link text-white" %>
              </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <%= @inner_content %>
  </body>
</html>
